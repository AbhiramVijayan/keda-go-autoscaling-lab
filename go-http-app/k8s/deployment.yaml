apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: go-http
  name: go-http
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-http
  strategy: {}  
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: go-http
    spec:
      containers:
      - image: go-http-app:1.0
        imagePullPolicy: IfNotPresent 
        name: go-http-app
        ports:
          - containerPort: 8080
        resources: {}
status: {}
# eval $(minikube docker-env)
# kubectl apply --server-side -f .